import 'package:flutter/material.dart';

class DonorsTab extends StatelessWidget {
  const DonorsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Donors tab'),);
  }
}
